# D09 · 认证依赖注入 + 受保护路由 GET /api/auth/me · 2026-09-09

## 本日文件影响清单

### 将新增
- `backend/app/core/deps.py`
- `backend/tests/test_me.py`

### 将改动
- `backend/app/routers/auth.py`（加 `GET /me`）
- `backend/app/core/security.py`（仅模块 docstring：密码哈希 → 密码哈希 + JWT 签发/校验）
- `.plan/daily/2026-09-09-d09.md`（本条，实现/验收/复盘记录）
- `.plan/schedule.md`（老师 review 后翻转 D09 ✅）

### 将删除
- 无

## 今日目标
- 用 `Depends` + `OAuth2PasswordBearer` 把 D08 的解码器接成鉴权依赖 `get_current_user`。
- 加第一个受保护接口 `GET /api/auth/me`，返回当前登录用户（UserOut，绝不含 hashed_password）。
- 用测试钉住：无 token / 坏 token / 过期 token / 合法 token / 用户已删 五种情况。
- 理清 401（未认证）vs 403（无权限）语义，纠偏 D08 遗留说法。

## 今日产出

- 新增 `OAuth2PasswordBearer` 与 `get_current_user` 认证依赖。
- 新增 `GET /api/auth/me`，返回当前用户公开资料。
- 认证失败统一返回 401，并附 `WWW-Authenticate: Bearer`。
- 测试覆盖缺失 token、无效 token、过期 token、合法 token 和用户已删除五种情况。

## 验收记录

- Ruff：`All checks passed!`
- pytest：`21 passed`（包含 D04-D08 全部回归测试）
- Alembic：`No new upgrade operations detected`
- MySQL、PostgreSQL 容器均为 `healthy`

## 关键设计决定

- `OAuth2PasswordBearer` 负责从 Authorization Bearer 头提取 token，并在缺失或格式错误时短路返回 401。
- JWT 验签保持无状态、不查库；`/me` 再按 subject 查库，是为了物化用户资料并阻止已删除用户继续访问。
- 过期、无效、缺失 token 都属于未认证，统一使用 401；403 留给后续权限控制场景。
- `UserOut` 显式限制公开字段，响应不会包含 `hashed_password`。

## 复盘（老师带教后填写）

## 老师评审结论（Claude，2026-09-10 追加）
**✅ 通过，无阻塞问题。** 在 backend/.venv（pip）实测：ruff `All checks passed!` · pytest **21 passed**（16 原有回归 + test_me 5 条）· alembic check `No new upgrade operations detected` · 全仓无 `print`、无 403 误用。

文件影响清单核验：新增 `core/deps.py`、`tests/test_me.py` ✅；改动 `routers/auth.py`、`security.py`（**确为仅 docstring 一行**）✅；`schedule.md` Codex 未动 ✅——**这次没有抢翻课表，过程问题已改正，表扬**。清单外增删改：无。

做得好的点：
- **分层干净**：router 里 `read_current_user` 只 `return current_user`，零 SQL 零业务；DB 查询落在 `core/deps.py`（依赖层），符合「core/deps 仅被上层调用」。
- **401 语义贯彻到底**：抽出 `authentication_error()` 统一造 401 + `WWW-Authenticate: Bearer`，三处失败出口（过期/无效/已删）都带 header，没有一处漏。
- **过期 vs 无效 detail 分开**（"请重新登录" vs "无效的访问令牌"）——服务端语义统一 401，但给前端留了分辨线索；403 一处没用，边界守住了。
- **防已删用户**：`db.get` 查不到就 401。这条不是模板套路，是真安全考量（token 有效 ≠ 主体仍存在），能想到说明理解了 JWT 无状态的局限。
- **注释点题**：`deps.py:44` 那句把「验签不查库」与「/me 查库」的和解点写在一行注释里，正是本课的题眼。
- 测试全部走**真实 HTTP 面**（TestClient + 真注册/真登录/真删库），不是 mock，5 类场景各钉一个断言。

评审发现（均为低，不返工）：
1.（低·测试缺口）`deps.py:39-42`「sub 非数字 → 401」分支已实现但**无测试**；可用 `settings.jwt_secret` 手工 `jwt.encode({"sub":"abc",...})` 补一条，与 test_token 的造坏 token 手法一致。
2.（低·D10 处理）`test_me.py` 与 `test_login.py` 重复了 `unique_identity` / `remove_user` / 注册辅助——**D10 正是测试日**，届时收进 `tests/conftest.py` 共享 fixture 更合适，本日不动。
3.（低·记录）`deps.py:45` 的 `db.get(User, ...)` 直接落在 core；与现有 service 内联查询风格一致，不算越界；将来真上 repository 层时，这一查可随之外移。仅备忘。

**D09 完成 → 鉴权链路闭环**：注册(D06) → 登录签发(D07) → 验签(D08) → **拦截受保护接口(D09)**。M1 只剩 D10（auth 测试收拢 + 回归 + M1 评审）。
