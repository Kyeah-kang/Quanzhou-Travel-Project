# D08 · JWT 解码校验工具 + Header.Payload.Signature 原理 · 2026-09-09

## 本日文件影响清单

### 将改动

- `backend/app/core/security.py`
- `.plan/modules/D08.md`
- `.plan/schedule.md`
- `.plan/daily/2026-09-09-d08.md`

### 将新增

- `backend/tests/test_token.py`

### 将删除

- 无

## 今日目标

- 补齐 JWT 验签、过期检查和 `sub` 必需声明检查。
- 将 PyJWT 异常收敛为业务层可识别的过期/无效两类异常。
- 用测试验证合法、过期、篡改、错误密钥和缺少 `sub` 的 token。

## 验收记录

- [x] Ruff 和 pytest 通过：Ruff `All checks passed!`，pytest `16 passed`
- [x] 合法 token 解码成功，返回字符串 subject
- [x] 过期 token 与其他无效 token 能区分
- [x] 未修改 main、路由、配置和数据库；Alembic `No new upgrade operations detected`

## 今日产出

- 增加 `TokenError`、`TokenExpiredError`、`TokenInvalidError` 三层异常。
- 增加 `decode_access_token`：验签、检查过期、要求 `sub/exp` 并返回 subject。
- 测试覆盖合法、过期、签名篡改、错误密钥和缺少 `sub` 五种情况。

## 关键设计决定

- `algorithms` 只接受服务端配置的算法，不信任 token header，防止算法混淆攻击。
- PyJWT 异常不会直接泄露给上层，统一转换为过期或无效两类业务异常。
- JWT payload 可被 base64url 解码查看，因此只放身份 subject 和过期时间，不放密码等敏感数据。
- 验签只依赖服务端密钥，不查数据库，保持 token 校验的无状态特性；鉴权依赖留给 D09。

## 复盘

### 我的没懂点

（____________）

### 老师讲解 / 我的复述记录

（____________）
