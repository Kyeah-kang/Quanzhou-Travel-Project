# D05 · 全表建模 + Alembic 迁移 · 2026-09-06

## 本日文件影响清单

### 将改动

- `backend/pyproject.toml`
- `backend/app/models/__init__.py`
- `.plan/daily/2026-09-06-d05.md`（补充验收与复盘记录）
- `.plan/modules/D05.md`（收尾更新状态与验收记录）
- `.plan/schedule.md`（收尾更新 D05 状态）

### 将新增

- `backend/app/models/heritage_site.py`
- `backend/app/models/chat_session.py`
- `backend/app/models/chat_message.py`
- `backend/app/models/itinerary.py`
- `backend/app/models/favorite.py`
- `backend/alembic.ini`
- `backend/alembic/README`
- `backend/alembic/env.py`
- `backend/alembic/script.py.mako`
- `backend/alembic/versions/.gitkeep`
- `backend/alembic/versions/` 中由 autogenerate 生成的首版迁移文件

### 将删除

- 无文件删除。`drop_all` 只清理 D04 临时数据库表，不删除仓库文件。

## 今日目标

- 补齐 MySQL 业务库的 6 张业务表 ORM 模型，并保持 D04 的 `users` 字段口径不变。
- 接入 Alembic，让 schema 通过迁移版本管理，而不是依赖 `create_all`。
- 验证首版迁移的 upgrade、downgrade 和再次 upgrade 都可执行。

## 验收记录

- [x] 依赖安装、Ruff 和 pytest 通过：Ruff `All checks passed!`，pytest `4 passed`
- [x] 迁移由 `alembic revision --autogenerate -m "init all tables"` 生成，并 upgrade 到 head
- [x] MySQL 中存在 6 张业务表和 `alembic_version`
- [x] `downgrade -1 -> upgrade head` 循环通过，外键和联合唯一约束存在

## 今日产出

- 新增 `heritage_sites`、`chat_sessions`、`chat_messages`、`itineraries`、`favorites` 五个 ORM 模型，与 D04 的 `users` 合计六张业务表。
- 接入 Alembic：`env.py` 从 `settings.mysql_dsn` 获取连接地址，ini 不保存数据库密码。
- 生成首版迁移：`backend/alembic/versions/5a8524575303_init_all_tables.py`。
- 清理 D04 的临时 `create_all` schema，并由 Alembic 重新建立正式 schema。

## 关键设计决定

- `mode`/`role` 使用 `String`，避免新增业务枚举值时必须修改数据库原生 ENUM。
- 别名、标签、消息来源、行程参数和行程内容使用 JSON，保留结构化数据的扩展空间。
- 外键列统一建立索引；收藏使用 `(user_id, site_key)` 联合唯一约束防止重复收藏。
- 自动生成的 downgrade 首次在 MySQL 上因先删除外键依赖索引触发 1553；迁移文件仅对 downgrade 做必要调整，改为按子表到父表直接删表，之后回滚/重建成功。
- `compare_server_default` 未启用，避免 MySQL 将 `func.now()` 反射成 `CURRENT_TIMESTAMP` 后产生无意义差异；`alembic check` 已通过。

## 复盘

### 我的没懂点

（____________）

### 老师讲解 / 我的复述记录

（____________）

## 老师评审结论（Claude，2026-09-06 追加）
**✅ 通过，无阻塞问题。** 在 backend/.venv（pip）实测：ruff 干净 · pytest `4 passed` · `alembic current`= `5a8524575303 (head)` · `alembic check` **No new upgrade operations detected（模型与库零漂移）** · MySQL 实见 7 表、`chat_messages` FK `ON DELETE CASCADE`、`favorites` 联合唯一 `uq_favorites_user_site`。

做得好的点：
- 6 模型字段口径与简报一致；FK 列 index、JSON 列、`mode/role` String、`chat_messages/favorites` 无 updated_at
- env.py URL 单点取自 `settings.mysql_dsn`，还做了 `% → %%` 转义防 ConfigParser 插值（密码含 `%` 也不炸）；alembic.ini `sqlalchemy.url=` 留空不存密码
- 迁移 upgrade 按 FK 父子顺序建表；downgrade 手调为**子表→父表直接删表**并注释了 MySQL 1553 原因——人工微调合理、已说明
- `alembic check` 零漂移证明「模型即真源」

评审发现（均低，不返工）：
1.（过程）schedule D05 被 Codex 在 review 前就翻成了 ✅、且 .plan docs 混进了 feat 提交。本次确已通过故 ✅ 保留，但约定「课表翻转留给老师」，docs 最好独立 docs: 提交。
2.（提示）`models/__init__.py` 的 `# noqa: E402` 其实不必要（import 本就在顶部），可合并成一条 `from app.models import user, chat_session, ...`；纯整洁，非必须。
3.（提示）坐标 lat/lng 用 Float（按简报取舍）；若日后要做精确地理计算可再评估 Decimal。
